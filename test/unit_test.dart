/// Smoke test du runner CI — remplacé par les suites métier.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('suite de tests métier chargée', () {
    expect(true, isTrue);
  });
}
